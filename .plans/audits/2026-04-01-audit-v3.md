# Audit Report -- 2026-04-01 (v3)

## Executive Summary
- **Packages analyzed**: shared, app, extension, api
- **Critical**: 0 | **High**: 0 | **Medium**: 3 | **Low**: 3
- **Dead code**: 13 unused files (main packages), 43 unused export groups, 50 unused type groups, 0 genuine unused deps
- **Lint errors**: 0
- **Type errors (source only)**: shared 0, app 0, extension 0
- **Architectural anti-patterns**: 35 files >500 lines (non-test, non-dist), 0 package .env files
- **TODO markers**: 2
- **Mode**: Single-agent

### Key Headlines

**Active refactoring in progress.** 104 uncommitted changes across all packages. Notable improvements since v2:
- `getCoops()` in `context.ts` now handles bad coop records gracefully (try/catch + doc cleanup) instead of crashing on corrupt state
- New `queueFollowUp()` fire-and-forget utility replaces blocking `await` for non-critical follow-ups (badge refresh, agent cycle poke) -- improves response latency in capture, review, and receiver handlers
- `ErrorBoundary` now logs `componentDidCatch` with component stack -- previously silent
- `requestAgentCycle` switched from try/catch to void promise chain -- prevents unhandled rejection in offscreen messaging
- `SidepanelIntentSegment` type expanded with `drafts`, `signals`, `stale` variants
- `RuntimeRequest` `category` field narrowed from `string` to `ReviewDraft['category']`

**Previous M2 finding fully resolved.** `partnerMarks` and `heroSignalFragments` removed from landing-data.ts. Replaced by different set of unnecessarily exported internal helpers (see M2 below).

**Codebase health stable.** 0 type errors, 0 lint errors, 0 genuine unused deps. Resolution velocity positive.

**Chronic deferred item**: M1 (ZK proof `null`) at 6 cycles. DEFERRED due to MV3 constraints, flagged for executive visibility.

---

## Previous Findings Status

_Tracked from: 2026-04-01 (v2)_

### High Findings
| ID | Finding | File | Status | Notes |
|----|---------|------|--------|-------|
| (none) | -- | -- | -- | No HIGHs in v2 |

### Medium Findings
| ID | Finding | File | Status | Notes |
|----|---------|------|--------|-------|
| M1 (v2) | ZK proof hardcoded to `null` | `review.ts:66` | **DEFERRED** (6 cycles) | Still present. Structural MV3 limitation. DEFERRED status carried forward. |
| M2 (v2) | Landing page unused exports (`partnerMarks`, `heroSignalFragments`) | `landing-data.ts` | **FIXED** | Both exports removed. New set of unnecessarily exported internal helpers flagged (see M2 below). |
| M3 (v2) | `app.tsx` approaching god object threshold (833 lines) | `app.tsx` | **STILL OPEN** (2 cycles) | Still 833 lines. No change. |

---

## Medium Findings

### M1. ZK proof generation hardcoded to `null` [DEFERRED, 6 cycles]
- **File**: `packages/extension/src/background/handlers/review.ts:66`
- **Issue**: Anonymous publish sets `membershipProof = null` with TODO. Snarkjs WASM workers require `URL.createObjectURL` and `new Worker`, both unavailable in MV3 service workers.
- **Status**: DEFERRED. The fix requires either (a) offscreen document delegation for ZK operations, or (b) a different ZK library compatible with MV3 constraints.
- **Rationale**: Structural platform limitation, not a code quality issue. Anonymous publishing works -- it just skips the proof. Stops escalation.
- **Note**: 6 cycles. DEFERRED so escalation does not apply, but flagged in Executive Summary as chronic deferred item.

### M2. Unnecessarily exported internal helpers [NEW]
- **Files**:
  - `packages/app/src/views/Landing/landing-data.ts:386,397,403,407,425` -- `createEmptyLandingDraft`, `isTranscriptKey`, `isAudienceId`, `mergeSetupInput`, `mergeTranscripts`
  - `packages/app/src/dev-environment.ts:1,3` -- `DEV_STATE_PATH`, `DEV_ACCESS_STORAGE_KEY`
- **Issue**: These functions/constants are exported but only consumed within their own files. The `export` keyword is unnecessary and inflates the module's public API surface.
- **Impact**: Minor. Increases apparent API surface and triggers knip false positives.
- **Recommendation**: Remove the `export` keyword from these 7 declarations. They are purely internal helpers.

### M3. `app.tsx` approaching god object threshold [STILL OPEN, 2 cycles]
- **File**: `packages/app/src/app.tsx` (833 lines)
- **Issue**: The main app component contains route resolution, dev environment management, all receiver views, error boundary, and multiple inline components.
- **Impact**: Maintainability. Adding new routes or receiver features further inflates this file.
- **Recommendation**: Extract `ErrorBoundary`, `DevGate`, and route resolution into separate files.

---

## Low Findings

### L1. 35 files exceed 500 lines (non-test, non-dist source)
- **Top offenders**: `agent-runner-skills.ts` (1565), `Landing/index.tsx` (1554), `archive.ts` (1483), `NestTab.tsx` (1372), `usePopupOrchestration.ts` (1173)
- **Change from prior**: Up 1 (from 34 to 35). `action-payload-parsers.ts` (982) newly visible in the count after shared module growth.
- **Net assessment**: File complexity stable. Same top offenders persist.

### L2. 2 TODO markers (unchanged)
- `extension/background/handlers/review.ts:66` -- ZK proof delegation (same as M1, DEFERRED)
- `extension/background/dashboard.ts:119` -- Missing blue icon assets

### L3. Production `as any` count: 1 (unchanged)
- `agent-models.ts:215` -- transformers pipeline (ACCEPTED)

---

## Dead Code (knip results, main codebase only)

### Unused Files (13, unchanged)
| File | Status |
|------|--------|
| `packages/app/public/sw.js` | Service worker stub -- loaded from HTML, not TS |
| `packages/extension/entrypoints/agent-webllm-worker.ts` | WXT entrypoint (knip can't trace) |
| `packages/extension/entrypoints/background.ts` | WXT entrypoint (knip can't trace) |
| `packages/extension/entrypoints/inference-worker.ts` | WXT entrypoint (knip can't trace) |
| `packages/extension/entrypoints/receiver-bridge.content.ts` | WXT content script (knip can't trace) |
| `packages/extension/src/background.ts` | Service worker entry (manifest reference) |
| `packages/extension/src/catalog/CatalogApp.tsx` | Catalog UI -- not wired yet |
| `packages/extension/src/catalog/main.tsx` | Catalog UI -- not wired yet |
| `packages/extension/src/runtime/agent-webllm-worker.ts` | Worker loaded via `new Worker()` |
| `packages/extension/src/views/Popup/main.tsx` | HTML entrypoint (knip can't trace) |
| `packages/extension/src/views/Sidepanel/main.tsx` | HTML entrypoint (knip can't trace) |
| `packages/extension/vite.catalog.config.ts` | Vite config (not TS import) |
| `packages/extension/wxt.config.ts` | WXT config (not TS import) |

**Genuinely dead**: 0 source files. Catalog stubs (`CatalogApp.tsx`, `catalog/main.tsx`, `vite.catalog.config.ts`) are intentional development tools. All other files are HTML/manifest/worker entrypoints that knip cannot trace.

### Unused Dependencies (0 genuine)
| Package | Dependency | knip Status | Actual Status |
|---------|-----------|-------------|---------------|
| docs | `@docusaurus/theme-common` | Unused | Consumed by Docusaurus internals |
| extension | `permissionless` | Unused | Used through `@coop/shared` + wxt build plugin |
| extension (dev) | `@wxt-dev/module-react` | Unused devDep | Used in `wxt.config.ts` modules array |

### Unused Exports (43 groups, unchanged)
Same pattern as prior audit. Most are extension background handler functions consumed via `chrome.runtime.onMessage` dispatch (not static imports). 7 new genuinely unnecessary exports in app package (landing-data.ts and dev-environment.ts internal helpers) -- see M2.

### Unused Exported Types (50 groups, unchanged)
Primarily component prop types (`*Props`, `*Deps`) and hook state types. Many are consumed only by co-located files or spread via `React.ComponentProps<>`.

---

## Architectural Anti-Patterns

| Anti-Pattern | Location | Lines | Change vs Prior | Severity |
|--------------|----------|-------|-----------------|----------|
| God Object | `extension/runtime/agent-runner-skills.ts` | 1565 | Unchanged | MEDIUM |
| God Object | `app/views/Landing/index.tsx` | 1554 | Unchanged | MEDIUM |
| God Object | `extension/background/handlers/archive.ts` | 1483 | Unchanged | MEDIUM |
| God Object | `extension/views/Sidepanel/tabs/NestTab.tsx` | 1372 | Unchanged | MEDIUM |
| God Object | `extension/views/Popup/hooks/usePopupOrchestration.ts` | 1173 | Unchanged | MEDIUM |
| God Object | `shared/modules/archive/archive.ts` | 1117 | Unchanged | MEDIUM |
| God Object | `shared/modules/coop/flows.ts` | 1026 | Unchanged | MEDIUM |
| Growing File | `app/src/app.tsx` | 833 | Unchanged, 2 cycles | LOW |
| Unused Exports | `landing-data.ts`, `dev-environment.ts` (7 internal helpers) | -- | NEW (different set) | LOW |

---

## Trend (last 5 audits)

| Metric | 2026-03-19 | 2026-03-24 (v2) | 2026-04-01 (v1) | 2026-04-01 (v2) | **2026-04-01 (v3)** |
|--------|-----------|-----------|-----------|-----------|-----------|
| Critical | 1 | 0 | 0 | 0 | **0** |
| High | 7 | 5 | 0 | 0 | **0** |
| Medium | 8 | 11 | 6 | 3 | **3** |
| Low | 3 | 3 | 3 | 3 | **3** |
| Unused files (knip) | 31 | 22 | 14 | 13 | **13** |
| Unused exports (knip) | 20 groups | 31 groups | 43 groups | 43 groups | **43 groups** |
| Unused types (knip) | 13 groups | 34 groups | 50 groups | 50 groups | **50 groups** |
| Unused deps (knip) | 7 | 1 | 0 genuine | 0 genuine | **0 genuine** |
| Lint errors | 24 | 364 | 1 | 0 | **0** |
| Source type errors | ~30 | 58 | 0 | 0 | **0** |
| God objects (>500 lines, non-test) | 15 | 23 | 35 | 34 | **35** |
| Production `as any` | -- | -- | 2 | 1 | **1** |
| Findings fixed | -- | 8 | 14 | 3 | **1** |
| Findings opened | -- | 5 | 3 | 2 | **1** |
| Resolution velocity | -- | 1.60 | 4.67 | 1.50 | **1.00** |

**Observations**:
- **Resolution velocity 1.00**: Neutral -- 1 finding fixed, 1 new. Debt neither growing nor shrinking.
- **Codebase health plateau**: Zero type errors, zero lint errors, zero genuine unused deps -- all held stable across 3 consecutive audit cycles. The codebase is in good shape.
- **God object count +1**: `action-payload-parsers.ts` (982 lines) newly crosses the 500-line visibility threshold in the detailed count. No structural change.
- **Active refactoring improving resilience**: The `getCoops()` robustness fix, `queueFollowUp()` fire-and-forget pattern, and improved error logging (`ErrorBoundary.componentDidCatch`, background runtime error logging) are all positive architectural improvements that don't show up as finding deltas but improve production reliability.
- **Chronic deferred item**: M1 (ZK proof `null`) at 6 cycles. DEFERRED due to MV3 constraints.
- **Unused exports/types stable**: 43/50 groups unchanged. Most are runtime-dispatched handlers invisible to static analysis.

---

## Recommendations (Priority Order)

1. **Remove unnecessary `export` keywords** -- 7 internal helpers in `landing-data.ts` and `dev-environment.ts` are exported but only used within their own files. Trivial cleanup. (Medium, M2)

2. **Consider splitting `app.tsx`** -- Extract `ErrorBoundary`, dev-gate logic, and receiver orchestration into separate files before it crosses the 1000-line threshold. 2 cycles open. (Medium, M3)

3. **Commit pending changes** -- 104 uncommitted modifications include robustness improvements (getCoops error handling, queueFollowUp pattern, ErrorBoundary logging) that should be committed. (Operational)

4. **Prune worktrees** -- 6+ active worktrees inflate knip counts by ~7x. Run `git worktree prune` after agents complete. (Operational)

5. **Track ZK proof delegation** -- M1 at 6 cycles, DEFERRED. Consider creating a GitHub issue to track offscreen-document ZK as a future milestone. (Informational)

---

## Next Steps

> **This audit is read-only.** To apply fixes, reply with:
> - `fix M2` -- remove unnecessary export keywords (7 declarations)
> - `fix M3` -- split app.tsx
> - `fix all` -- address all findings by priority
> - `fix M2, M3` -- address specific findings by ID
